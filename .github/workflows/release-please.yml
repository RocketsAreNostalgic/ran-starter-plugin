name: Release Please

on:
    workflow_run:
        workflows: [Quality]
        types: [completed]
        branches: [main]

permissions: {}

jobs:
    release:
        permissions:
            contents: write
            issues: write
            pull-requests: write
        uses: RocketsAreNostalgic/.github/.github/workflows/release-profile-a.yml@d758380ba2c1eefd2196ca66028661e189db56e0
        with:
            expected-workflow-path: .github/workflows/quality.yml
