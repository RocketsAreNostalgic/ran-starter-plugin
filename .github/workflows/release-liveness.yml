name: Release Liveness

on:
  schedule:
    - cron: '17 * * * *'
  pull_request:
    paths:
      - .github/workflows/release-liveness.yml
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  observe:
    uses: RocketsAreNostalgic/.github/.github/workflows/release-liveness-observer.yml@0d386009940c19cf493b96d2c3aece06c4db9e75
