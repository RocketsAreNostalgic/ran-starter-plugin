name: Release Liveness

on:
  schedule:
    - cron: '17 * * * *'
  pull_request:
    paths:
      - .github/workflows/release-liveness.yml
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  observe:
    uses: RocketsAreNostalgic/.github/.github/workflows/release-liveness-observer.yml@89a6b0eb3841d35a2f0899336782dd6564abb225
