name: Release Liveness

on:
  schedule:
    - cron: '17 * * * *'
  pull_request:
    paths:
      - .github/workflows/release-liveness.yml
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  observe:
    uses: RocketsAreNostalgic/.github/.github/workflows/release-liveness-observer.yml@9992d51546d6bb60a4272f5f71212cd821f80a35
