name: Release Liveness

on:
  schedule:
    - cron: '17 * * * *'
  pull_request:
    paths:
      - .github/workflows/release-liveness.yml
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  observe:
    uses: RocketsAreNostalgic/.github/.github/workflows/release-liveness-observer.yml@b10d7ebc572ebfd58e1dcd73814058a1165833ba
