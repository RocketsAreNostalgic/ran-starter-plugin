name: Release Liveness

on:
  schedule:
    - cron: '17 * * * *'
  pull_request:
    paths:
      - .github/workflows/release-liveness.yml
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  observe:
    uses: RocketsAreNostalgic/.github/.github/workflows/release-liveness-observer.yml@5fabd8c6cb9524e90ea22bb3dc031cde9bdc195d
